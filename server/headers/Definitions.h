#ifndef DEFINITIONS_H
#define DEFINITIONS_H

// These are the enumerations we are using for communication between Serializer and Controller
enum commands_t {ADD_CONTENT, ADD_INVOICE, GET_CONTENT, ERROR};

#endif
